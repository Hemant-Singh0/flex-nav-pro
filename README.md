# Responsive React Website

This project is a simple and responsive React website built using **React Router**, a custom **Navbar**, and modern CSS layout techniques such as **Flexbox** and **CSS Grid**.

---

## 🚀 Features

### ✅ Responsive Navbar (Flexbox)
- Built using `display: flex`
- Includes desktop links and a hamburger menu for mobile devices
- Uses `hamburger-react` for animated menu toggle

### ✅ 3-Column Card Layout (CSS Grid)
- Grid layout using:
  - `grid-template-columns`
  - `auto-fit / auto-fill`
  - `grid-gap`
  - `grid-areas`
- Fully responsive cards on Home page

---

## 🧩 Tech Stack
- **React.js**
- **React Router**
- **CSS Flexbox**
- **CSS Grid**
- **Hamburger React**
- **Vite**

---

## 🏗 How to Run the Project
### Install dependencies  
```bash
npm install

### Start development server
npm run dev

### Build for production
npm run build
